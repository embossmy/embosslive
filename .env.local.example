NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
ADMIN_PASSWORD=change-me
# CREW_PASSWORD is no longer used — each event has its own crew password,
# set in the admin UI ("Crew password (per-event)" field). Migration 002 adds
# the events.crew_password column.
